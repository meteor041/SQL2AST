# Core SQL parsing / AST distance
sqlglot>=25.0.0

# Evaluation and utility scripts
tqdm>=4.66.0
func-timeout>=4.3.5
scipy>=1.10.0
scikit-learn>=1.3.0

# Training scripts
PyYAML>=6.0.0
datasets>=2.18.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.0
accelerate>=0.28.0
sentencepiece>=0.2.0

# Optional helper used by cscsql.utils.file_utils.load_jsonl.
jsonlines>=4.0.0

# Install torch separately for the target CUDA version, for example:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
